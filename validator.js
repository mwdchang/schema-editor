class Validator {

  constructor() {
    this.handle = null;
  }

  requestValidation(txt) {
    if (this.handle) {
      clearTimeout(this.handle);
    }
    setTimeout(()=> {
    }, 2500);
  }





}
